Source: pt-touchscreen
Section: utils
Priority: optional
Maintainer: pi-top <deb-maintainers@pi-top.com>
Build-Depends: debhelper-compat (= 12)
Standards-Version: 4.5.0
Homepage: https://pi-top.com/

Package: pt-touchscreen
Architecture: all
Depends: ${misc:Depends},
# 'cut', 'tail' commands
 coreutils (>= 8.30-3),
# 'grep' command
 grep (>= 3.3-1),
# Used when waiting for breadcrumb from udev rule
 inotify-tools,
# Possibly used for notifications
 libnotify-bin,
# Gesture support
 touchegg,
# Used for 'xset' and 'xrandr' commands
 x11-xserver-utils,
# pt-ui-mods provides a notification-wrapped call to touchegg
Suggests: pt-ui-mods,
# Notifications that can be interacted with
 pt-notifications
# Avoid errors since touchscreen udev rule was provided by
# pt-os-mods in versions < 8.0
Replaces: pt-os-mods (<< 8)
Breaks: pt-os-mods (<< 8)
Description: pi-top Touchscreen Support
 Dependency package for configuring anything relating to
 touchscreen support on pi-topOS.
